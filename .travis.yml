language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-r' ]; then cd .. && eval "mv $currentfolder generator-r" && cd generator-r; fi
